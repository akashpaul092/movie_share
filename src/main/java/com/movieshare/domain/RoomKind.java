package com.movieshare.domain;

public enum RoomKind {

	/** YouTube sync watch party (+ chat, optional webcam). */
	WATCH_PARTY,

	/** Meet & share: WebRTC, screen share (no YouTube host sync). */
	MEET
}
